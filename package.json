{
  "name": "infographic-screenshot-app",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^24.10.0"
  },
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install --with-deps"
  }
}
